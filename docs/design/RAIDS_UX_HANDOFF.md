# Рейды — UX/UI handoff задачи #53

Статус: готово к просмотру Павлом. Реализация остаётся дизайн-прототипом и не подключена к production API.

## Цель экрана

Дать участнику Кабанды один понятный центр управления рейдами: быстро вернуться в активный рейд, увидеть ближайшие планы, выбрать готовый маршрут и найти завершённые поездки.

## Визуальное направление

Главный референс — мобильный макет, переданный Павлом 31 августа 2026 года. Перенесены его композиция, красно-чёрная иерархия, плотность, секционные заголовки, карточка активного рейда, компактные строки и карточки маршрутов. После мобильного ревью карточки маршрутов собраны в сетку `2 × N`.

Сознательные продуктовые отличия:

- внизу остаются четыре существующих раздела `Главная / Карта / Рейды / Кабанда`, а не пять разделов из референса;
- системная строка iOS не рисуется внутри HTML — её даёт устройство;
- изображения маршрутов временные и используют существующие материалы Кабанды;
- минимальные интерактивные цели сохранены около 44 px даже там, где референс визуально плотнее.

## Информационная архитектура

1. `Сейчас`: один активный рейд или компактное пустое состояние.
2. `Предстоящие`: только рейды, на которые текущий пользователь уже записан; если таких нет, секция целиком скрыта.
3. `Доступные маршруты`: сетка из двух карточек в строке без горизонтального скролла; новые карточки продолжают сетку ниже.
4. `История`: визуальные карточки завершённых рейдов с достижением, обложкой, составом, метриками, датой и фотолентой; фильтры `Все / Мои / С маршрутами` работают локально.

Двухколоночная сетка выбрана после проверки на телефоне: все маршруты обнаруживаются обычной вертикальной прокруткой, а третий элемент не превращается в слишком узкую миниатюру. Вся карточка открывает preview; отдельной кнопки `Подробнее` нет.

## Основные переходы

- `Новый рейд` открывает bottom sheet с вариантами `Поехали сейчас` и `Запланировать`.
- `Запланировать` открывает отдельный шаг даты и времени, затем общий выбор участников.
- карточка готового маршрута открывает отдельный routable preview.
- `Поехать по маршруту` открывает отдельный экран выбора участников.
- быстрый старт ведёт в состояние открытого lobby, планирование — в подтверждение предстоящего рейда.
- Escape и нажатие на затемнение закрывают bottom sheet.
- выбранный маршрут и источник старта сохраняются в URL, поэтому reload и Back/Forward не меняют контекст.

## Component map

| Компонент | Назначение | Production-кандидат |
| --- | --- | --- |
| `RaidHub` | Композиция четырёх секций | Да |
| `ActiveRaid` | Текущий рейд и возврат | Да |
| `UpcomingRow` | Запланированный рейд | Да |
| `RouteCard` | Готовый маршрут | Да |
| `RaidHistorySection` | Фильтры и выдача завершённых рейдов | Да |
| `HistoryCard` | Обложка, достижение, состав, метрики и фотолента завершённого рейда | Да |
| `NewRaidSheet` | Выбор сценария создания | Да |
| `RoutePreview` | Детали маршрута | Да |
| `ScheduleRaid` | Дата и время планируемого рейда | Да |
| `ParticipantPicker` | Состав до старта | Да |
| `BottomNav` | Прототип четырёх текущих вкладок | Использовать существующий `AppTabBar` при production-интеграции |

## Визуальные токены

```text
Accent: #F42D29
Accent dark: #D92824
Ink: #17181B
Muted: #696B72
Border: #E4E4E7
Page gutter: 18 px (12 px at 320)
Section gap: 14 px
Card radius: 15–18 px
Primary control height: 44–50 px
Bottom navigation: 72 px + safe area
Font: Manrope Variable
Primary action: #D62F2A (контраст белого текста 4.88:1)
```

## State matrix

| Область | Состояния | Как открыть в прототипе |
| --- | --- | --- |
| Hub | active, idle | `variant=active`, `variant=idle` |
| Hub loading | skeleton | `variant=loading` |
| Предстоящие | скрыто без записей / только рейды пользователя | `variant=upcoming-empty`, `variant=upcoming-cases` |
| Каталог | error / partial error / fallback cover | `variant=catalog-error`, `variant=partial-error`, `variant=fallback` |
| Hub | empty / offline stale | `variant=empty`, `variant=offline` |
| Preview | normal / archived / partial error / fallback | `screen=preview`, дополнительно соответствующий `variant` |
| Планирование | дата и время | `screen=schedule` |
| Участники | default / 20 человек / retry error / permission error | `screen=participants`, `participant=long`, `participant=error`, `participant=permission-error` |
| Результат создания | lobby / scheduled | `screen=scheduled&origin=quick`, `screen=scheduled&origin=schedule` |
| New raid | bottom sheet | кнопка `Новый рейд` |

Для ручного ревью можно добавить `review=1`: появится служебный переключатель экранов и состояний. В продукте его нет.

## Доступность

- секции связаны с заголовками через `aria-labelledby`;
- icon-only кнопки имеют `aria-label`;
- bottom sheet имеет `role=dialog`, `aria-modal` и закрывается по Escape;
- focus trap удерживает клавиатурный фокус внутри sheet, после закрытия фокус возвращается на `Новый рейд`;
- фокус имеет заметный синий outline;
- состояния активного рейда обозначены текстом, а не только красным цветом;
- чекбоксы — нативные, CTA блокируется при пустом составе;
- горизонтального скролла страницы и секции маршрутов нет;
- фильтры истории — нативные кнопки с `aria-pressed`, число результатов озвучивается коротким `aria-live` сообщением;
- карточка истории имеет единое подробное accessible name, дата размечена через `time`;
- в шагах планирования и выбора состава global tab bar скрыт, чтобы не конкурировать с critical flow.

## Зафиксированный копирайт

`Сейчас`, `Вернуться в рейд`, `Предстоящие`, `Доступные маршруты`, `История`, `Первый рейд ещё впереди`, `Каталог временно недоступен`, `Как едем?`, `Поехали сейчас`, `Запланировать`, `Кто сегодня едет?`, `Повторить`, `Данные сохранены на телефоне`.

## Решённые продуктовые вопросы

- chooser реализован bottom sheet, а не modal;
- финальный каталог — двухколоночная сетка без горизонтального жеста и без отдельной кнопки `Подробнее`; вся карточка остаётся ссылкой;
- пустая секция `Предстоящие` не показывается; в ней остаются только рейды, на которые записан текущий пользователь (включая вожака собственного рейда);
- route preview — отдельный routable экран, а не sheet: так последовательность точек помещается без вложенного скролла;
- route template и реальный upcoming raid визуально разведены: изображение-каталог против компактной календарной строки;
- upcoming и history являются ссылками; календарь/флаг служат стабильным fallback при отсутствии cover;
- история повторяет композицию нового референса, но сохраняет 44 px touch targets и локальные бренд-изображения Кабанды вместо внешних фотографий;
- один `ParticipantPicker` используется для быстрого старта, маршрута и планирования;
- route cards сохраняют плотность референса: расстояние, длительность и количество точек видны сразу, полное описание раскрывается в preview и входит в accessible name;
- крестик закрытия, календарь и велосипед используют единый 24×24 outline SVG-стиль; видимый glyph отделён от 44×44 touch target;
- desktop использует те же четыре блока с сеткой 2 + 3 колонки, без отдельной информационной архитектуры.

## Ограничения дизайн-задачи

- нет вызовов API, сохранения в БД и изменения lifecycle рейда;
- нет production-CRUD шаблонов и административных ролей;
- данные и изображения демонстрационные;
- физический smoke на реальном телефоне и явное `UX approved` выполняются Павлом как финальный acceptance gate;
- интеграция с каноническими raid endpoints выполняется в следующих исполнительных задачах после `UX approved`.

## Критерий согласования

Для завершения #53 нужен явный комментарий Павла `UX approved` либо перечень конкретных правок. До этого issue остаётся открытым на ревью.
